apiVersion: v1
kind: Service
metadata:
  name: indexer-service
  namespace: linkpouch-dev
  labels:
    app: indexer-service
spec:
  selector:
    app: indexer-service
  ports:
  - port: 80
    targetPort: 8081
  clusterIP: None
