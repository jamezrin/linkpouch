apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: linkpouch-tls
  namespace: linkpouch-dev
spec:
  secretName: linkpouch-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - linkpouch.example.com  # Replace with your domain
